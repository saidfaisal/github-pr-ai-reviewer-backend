info:
  name: Valid Real PR Webhook
  type: http
  seq: 19

http:
  method: POST
  url: http://127.0.0.1:3000/webhooks/github
  headers:
    - name: Content-Type
      value: application/json
    - name: X-Github-Event
      value: pull_request
    - name: X-Github-Delivery
      value: local-test-014
  body:
    type: json
    data: |-
      {
        "action": "opened",
        "number": 1,
        "pull_request": {
          "number": 1,
          "head": {
            "sha": "3a1d9580cebbfd7e2d57cee91358af9fb4ca36f6"
          }
        },
        "repository": {
          "full_name": "saidfaisal/github-pr-ai-reviewer-test"
        },
        "sender": {
          "login": "saidfaisal"
        }
      }
  auth: inherit

runtime:
  scripts:
    - type: before-request
      code: |-
        const CryptoJS =
            require("crypto-js");

        const secret =
            bru.getEnvVar("webhookSecret");

        const rawBody =
            req.getBody({
                raw: true,
            });

        const signature =
            CryptoJS
                .HmacSHA256(
                    rawBody,
                    secret
                )
                .toString(
                    CryptoJS.enc.Hex
                );

        req.setHeader(
            "X-Hub-Signature-256",
            `sha256=${signature}`
        );

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
